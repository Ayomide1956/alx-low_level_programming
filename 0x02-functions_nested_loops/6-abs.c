#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 * @n: is the in to be calculated
 * Return: the absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
